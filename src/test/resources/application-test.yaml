micronaut.application.name: in-memory-db
aol:
  dir: ${AOL_DIR:src/test/resources/testLogs}
  filename: ${AOL_FILE_NAME:append-only-log}
  flushInterval: ${AOL_LOG_FLUSH_INTERVAL:2s}
  bufferSize: ${AOL_BUFFER_SIZE:8192}
  initialMapCapacity: ${AOL_INITIAL_MAP_CAPACITY:16}
